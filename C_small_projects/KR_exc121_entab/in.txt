#include <stdio.h>


// Function declaration
int str_compare(char str1[], char str2[]);

int main() {
---------char string1[100] = "Hello World!";
---------char string2[100] = "Hello World!";
---------int compare;

---------printf("\nString 1 = %s\nString 2 = %s\n", string1, string2);
---------printf("\nComparing...\n");
    
---------compare = (str_compare(string1, string2));
---------if (compare) {
------------------printf("> True\n\n");
---------}
---------else {
------------------printf("> False\n\n");
---------}
}

int str_compare(char str1[], char str2[]) {
---------int i = 0;

---------while (str1[i] == str2[i]) {
------------------if (str1[i] == '\0' && str2[i] == '\0') {
---------------------------return 1;
------------------}
------------------++i;
---------}
---------return 0;
}